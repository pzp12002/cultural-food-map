# 美食地图项目规划

## 1. 项目目标

制作一个以实拍美食照片为核心的响应式 Web 地图：

- 以一个固定地点为中心，默认总览半径约 10 公里内的实拍门店；关键词搜索可覆盖武汉市内更远的高德餐饮 POI。
- 当前中心为武汉市洪山区高新大道 758 号大悦中心 T1 写字楼微派，高德坐标为 `114.499625, 30.488575`。
- 地图首次打开时自动显示这一区域，不请求访问者的实时定位。
- 在高德地图上准确标出范围内的店铺位置。
- 地图标记直接展示实拍照片缩略图，照片下方带定位指针并锚定门店坐标。
- 点击照片后打开大图，同时展示店名、地址、菜品、介绍、营业信息等详情。
- 手机和桌面浏览器均可顺畅使用。

首期不做用户定位、分类筛选、登录、收藏、支付和运营后台。测试版支持通过高德 POI 搜索或点击底图地点查看真实商铺，为选中的商铺添加星级、文字评价和照片，并以微派为固定起点唤起步行导航；用户数据仅保存在当前浏览器。

## 2. 产品形态与核心体验

### 2.1 页面布局

桌面端：

- 地图占据主要视口。
- 顶部只保留项目名称和简洁说明。
- 门店照片以自定义 Marker 形式悬浮在对应坐标上方。
- 点击 Marker 后，右侧打开门店详情面板，地图仍保持可操作。

移动端：

- 地图全屏展示，顶部只保留项目名称。
- Marker 使用更小的照片缩略图，避免遮挡地图。
- 点击 Marker 后，从底部拉起门店详情面板。
- 点击详情中的主图进入全屏图片预览，可左右切换该店照片。

### 2.2 核心用户流程

1. 用户进入页面，地图自动显示固定中心点周边约 10 公里的范围。
2. 用户提交关键词后，通过 `AMap.PlaceSearch` 合并中心点附近检索与武汉市文本检索，不把餐饮类型和 10 公里半径作为唯一入口，再过滤出餐饮相关 POI。
3. 在店铺级缩放下点击高德底图地点，通过逆地理查询命中 45 米内最近 POI 并读取完整详情。
4. 地图与列表按距微派的距离展示合并去重后的搜索结果；清空搜索后恢复实拍门店和评价过的高德商铺。
5. 用户可以在这一区域内拖动或缩放地图；空白位置点击不会生成虚假商铺。
6. 点击照片 Marker，地图平滑居中到该门店并高亮标记。
7. 打开详情面板，查看照片、距离、地址、营业信息、项目评分和高德参考评分，并可唤起导航。
8. 点击大图进入全屏预览；关闭后回到门店详情。

### 2.3 地图拥挤处理

- 地图低于 16 级时，按屏幕网格自动合并距离过近的照片 Marker，并显示该组门店数量。
- 点击合并 Marker 后放大到展开级别，不直接误选组内第一家店。
- 地图达到 16 级后展示全部照片 Marker。
- 选中的 Marker 提升层级并显示店名，未选中的 Marker 保持紧凑。
- Marker 使用固定尺寸，图片裁切比例统一，防止加载后位置跳动。

## 3. MVP 功能范围

| 模块 | MVP 内容 | 后续扩展 |
| --- | --- | --- |
| 地图 | 高德地图加载、固定中心点、约 10 公里初始视野、缩放与拖动 | 自定义地图样式 |
| 门店标记 | 照片 Marker、定位指针、选中态、屏幕网格合并 | 动态推荐 |
| 门店详情 | 大图、图集、店名、距离、地址、营业时间、人均、电话、数据来源 | 标签、探店视频 |
| 图片体验 | 缩略图懒加载、全屏预览、左右切换 | 原图下载、图片故事说明 |
| 查找 | 高德 POI 关键词搜索、10 公里范围限制、最近 12 家结果 | 分类筛选、分页、距离排序切换 |
| 导航 | 以微派为固定起点，唤起高德步行导航 | 用户实时位置作为起点 |
| 内容管理 | 本地结构化商铺数据 | 独立后台、草稿与发布流程 |
| 用户能力 | 本地评分、文字评价、照片上传 | 多人共享、收藏、分享、登录 |

## 4. 推荐技术方案

### 4.1 前端

- Vue 3 + TypeScript + Vite。
- 高德地图 JavaScript API 2.0，通过 `@amap/amap-jsapi-loader` 加载。
- Vue Router 预留可分享的门店详情地址，例如 `/shop/:slug`。
- 状态保持轻量化；首期用组合式函数管理地图和当前门店，无需引入复杂全局状态。
- 图片预览优先使用成熟的无障碍 Lightbox 组件，或在既有 UI 组件库中选择对应组件。

选择原因：项目目前为空，Vue 方案体量轻、移动端适配直接，也与高德 JS API 官方示例路径匹配。MVP 可以构建为纯静态站点并部署到 CDN。

### 4.2 数据与内容

MVP 使用仓库中的 JSON 或 TypeScript 数据文件，由前端静态加载。建议数据模型：

```ts
interface Shop {
  id: string
  slug: string
  name: string
  category: string[]
  longitude: number
  latitude: number
  address: string
  summary: string
  signatureDishes: string[]
  businessHours?: string
  averagePrice?: number
  phone?: string
  coverImage: string
  gallery: Array<{
    src: string
    thumbnail: string
    alt: string
    caption?: string
  }>
  amapPoiId?: string
  published: boolean
}
```

坐标统一保存为高德使用的 GCJ-02 坐标。若原始数据来自 GPS/WGS-84，不应直接混用，应在导入环节统一转换并人工复核。

### 4.3 图片存储

- 开发阶段：使用仓库内的压缩样片。
- 上线阶段：使用对象存储 + CDN，不把大量原图打包进前端产物。
- 每张照片至少输出缩略图和展示图两档；缩略图建议 WebP/AVIF，详情图保留高质量 WebP/JPEG 回退。
- 保留原始照片独立归档，网页不直接加载原图。
- 每张图补充准确的 `alt` 文本，图片 EXIF 中的敏感位置信息在发布前清理。

### 4.4 是否需要后端

测试版不做后端，用户评价和压缩后的照片保存在浏览器 `localStorage`，只用于单机交互验证。满足以下任一条件后再引入内容管理服务：

- 门店超过约 100 家，前端一次性数据加载开始影响体验。
- 每周频繁更新门店或照片，修改代码发布不再高效。
- 需要多人编辑、审核、定时发布或下架。
- 需要多人共享评论、用户账号、内容审核或访问统计明细。

后续可选择轻量 CMS + 数据库 + 对象存储，前端接口保持 `Shop` 数据契约不变。

## 5. 高德开放平台准备

项目需要高德地图 JS API 2.0。即使页面只用于展示，浏览器仍然会调用高德地图 API，因此需要鉴权。官方当前说明是：2021-12-02 之后申请的 Web 端（JS API）Key 必须配合安全密钥 `securityJsCode` 使用，不能只使用新申请的 Key。

开发演示时可以把 `securityJsCode` 以明文方式放在前端代码中，不需要另外开发服务器，但高德明确说明这种方式不建议用于正式生产环境。正式上线时更安全的方式是由代理服务器转发请求并在服务器端保存安全密钥。

创建 Web 端 Key 的官方准备文档目前没有把“自有域名”列为创建 Key 的前置条件。开发时可运行在 `localhost`；上线时网站自然会有一个访问地址，可以直接使用托管平台提供的地址，不必现在购买独立域名。若控制台届时要求填写使用域名或来源限制，再填写实际部署地址即可。

上线前需要完成：

1. 在高德开放平台创建应用。
2. 为应用添加“Web 端（JS API）”Key。
3. 获取 Key 对应的安全密钥 `securityJsCode`。
4. 开发阶段使用 `localhost`；部署后记录网站的实际访问地址。
5. 核对调用配额、产品定价、技术服务许可和地图内容合规要求。

开发演示的环境变量：

```bash
VITE_AMAP_KEY=
VITE_AMAP_SECURITY_CODE=
```

注意：通过 `VITE_` 注入的值最终都会出现在浏览器代码中，不是真正的秘密。该方式只适合当前的本地开发和简单演示。浏览器端地图 Key 也不等同于服务器端 Web 服务 Key。

高德相关入口：

- [高德开放平台控制台](https://console.amap.com/dev/user/permission?from=person-alipay)
- [地图 JS API 2.0](https://lbs.amap.com/api/javascript-api-v2/summary)
- [JS API 加载说明](https://lbs.amap.com/api/javascript-api-v2/guide/abc/load)
- [JS API 安全密钥使用](https://lbs.amap.com/api/javascript-api-v2/guide/abc/jscode)
- [坐标拾取器](https://lbs.amap.com/tools/picker)

## 6. 信息与素材准备清单

每家店至少准备：

- 唯一编号和店名。
- 准确地址、经度、纬度，并在高德地图人工复核。
- 1 张封面图和建议 3-8 张详情图。
- 每张图对应的菜名或说明。
- 50-150 字门店简介。
- 菜系或主题标签、招牌菜、营业时间。
- 可选：人均价格、联系电话、高德 POI ID、导航链接。
- 门店及人物拍摄授权、图片使用授权。

推荐先用 8-15 家店做首批内容，足以验证 Marker 密度、移动端详情体验和图片加载性能。

## 7. 项目结构建议

```text
src/
  components/
    map/FoodMarker.vue
    map/MarkerCluster.vue
    shop/ShopDetailPanel.vue
    shop/ShopGallery.vue
    filters/ShopFilters.vue
  composables/
    useAmap.ts
    useShopSelection.ts
  data/
    shops.ts
  pages/
    MapPage.vue
  types/
    shop.ts
  styles/
public/
  images/
```

地图 SDK 的创建、事件注册和销毁集中在 `useAmap.ts`，页面组件不直接散落操作地图实例的逻辑。Marker 只接收门店数据和选中状态，详情面板只负责展示及交互。

## 8. 实施阶段与工期

以下为 1 名前端开发、素材基本齐全时的建议排期：

### 阶段 0：需求与素材确认，0.5-1 天

- 确认首发平台为响应式 Web/H5。
- 确定 10 公里范围的固定中心点，并确认范围内的门店和素材清单。
- 获取高德 Web 端 Key 与安全密钥。
- 用坐标拾取器复核所有门店坐标。

### 阶段 1：交互原型与视觉规则，1 天

- 完成桌面端、移动端地图页线框。
- 确定 Marker 尺寸、选中态和详情面板。
- 用真实照片验证图片裁切、地图遮挡和文字长度。

### 阶段 2：MVP 开发，2-4 天

- 初始化项目和环境配置。
- 接入高德 JS API 2.0。
- 完成固定中心点、10 公里初始视野、照片 Marker 和选中联动。
- 接入 `AMap.PlaceSearch` 周边餐饮 POI 搜索及加载、空结果、错误状态。
- 接入底图点击坐标的逆地理 POI 识别和详情查询。
- 完成详情面板、全屏图片预览、双评分、距离、导航、评分评论和照片上传。
- 保存评价过的高德商铺，使评价在刷新后仍可访问。
- 接入首批真实门店数据与图片。

### 阶段 3：质量与上线，1-2 天

- 手机与桌面响应式测试。
- 图片压缩、懒加载和弱网测试。
- 验证 Key、安全密钥、HTTPS 和错误兜底。
- 内容复核、授权检查和正式部署。

缩小功能范围后的 MVP 总工期预计 5-8 个工作日；若素材尚未整理或需要重新拍摄，时间另计。

## 9. 验收标准

### 功能验收

- 所有已发布门店在正确坐标显示，Marker 与门店数据一一对应。
- 地图首次打开时显示固定中心点周边约 10 公里，范围外门店不进入首期数据。
- 提交关键词后能合并附近及武汉市文本搜索中的高德餐饮 POI，结果可超出 10 公里，清空搜索后恢复实拍门店。
- 店铺级缩放下点击高德底图地点可以打开最近 POI 详情，点击空白位置不会生成商铺。
- 点击 Marker 后，详情中店名、照片和地址正确，无串店。
- “我们的评分”和“高德参考评分”分开计算和标注，距离与导航目标坐标准确。
- 点击详情主图可以放大，支持关闭和图集切换。
- 提交评价后，评分、评论数、评论列表和地图照片合集同步更新，刷新页面后数据仍保留。
- 地图加载失败、Key 配置错误、图片失败和空结果都有明确兜底状态。

### 体验与性能验收

- 360px 宽移动端到常见桌面尺寸均无内容重叠或横向溢出。
- Marker 图片未加载时占位尺寸稳定，地图不会跳动。
- 首屏不加载全部高清原图；详情图片按需加载。
- 键盘可关闭详情和图片预览，按钮有可读名称，图片有替代文本。
- 地图实例、事件和自定义 Marker 在页面卸载时正确销毁。

### 数据验收

- 经纬度统一为 GCJ-02，并逐店人工抽检。
- 照片拥有发布授权，敏感 EXIF 信息已处理。
- 电话、营业时间和价格等易变信息标注最后核对日期。

## 10. 主要风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| Key 或安全密钥配置错误 | 地图无法加载 | 在 `localhost` 和最终部署地址分别做冒烟测试 |
| 坐标系混用 | 门店标记发生偏移 | 全部转为 GCJ-02，并逐店人工复核 |
| Marker 过多或照片过大 | 地图卡顿 | 聚合、视野内渲染、缩略图、懒加载 |
| 图片遮挡地图 | 浏览困难 | 固定尺寸、随缩放切换展示密度、选中项才显示更多信息 |
| 店铺信息变化 | 内容失真 | 增加核对日期，建立定期复核与下架流程 |
| 地图或照片授权不完整 | 上线合规风险 | 上线前核对高德许可和拍摄、肖像、商户授权 |
| 用户定位引发隐私要求 | 增加授权和合规成本 | MVP 使用固定中心点，不请求访问者定位 |

## 11. 第二阶段候选功能

- 使用访问者实时位置计算“离我最近”并作为导航起点。
- 美食分类、行政区、营业中、人均价格筛选。
- 主题路线，例如早餐路线、夜宵路线、老字号路线。
- 门店分享卡片和可索引的独立详情页。
- 内容管理后台、批量图片上传、门店草稿和审核发布。
- 访问数据统计，包括 Marker 点击、详情打开和导航转化。

## 12. 开工前需要确认的决策

当前方案已经确定为响应式 Web/H5、固定 10 公里范围、照片 Marker、高德 POI 搜索与底图地点详情、本地评分评论、照片上传和固定起点导航。继续补充实拍数据时只需要提供：

1. 10 公里范围的中心位置，可以是一个地址或一个高德地图坐标。
2. 首批门店的名称、地址和照片；门店坐标可以在开发前统一整理。
3. 照片和店铺信息可以公开展示的授权确认。

实施方案为“固定 10 公里范围 + 高德 POI + 照片 Marker + 右侧/底部详情面板 + 浏览器本地评价数据”。
